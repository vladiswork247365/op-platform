# Движок монтажа Reels-фабрики. Всё ставится с pypi.
imageio-ffmpeg>=0.5   # статический ffmpeg-бинарник (рендер)
Pillow>=10           # рендер субтитров/хуков в PNG
pillow-heif>=0.16    # чтение .HEIC с айфона
numpy>=1.26
