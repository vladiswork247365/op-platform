# Про-арсенал виральности (ставится на сервере/компе с сетью — модели/бинарники тянутся)
auto-editor>=27     # авто-динамика: режет паузы/тишину, джамп-каты (8K⭐)
librosa>=0.10       # детекция битов музыки → нарезка в ритм
ruaccent>=1.5       # ⭐ русские ударения для TTS (нейромодель+словарь, снимает омографы).
                    #   Без него работает встроенный словарь montage/stress_dict.json.
# faster-whisper — в requirements-subs.txt (пословные субтитры)
# Pexels-сток — stdlib (нужен только env PEXELS_API_KEY)
