---
description: Смонтировать ролик — нарезка, вырезание пауз, ритм, переходы
---

Используй скилл `video-edit`.

Пожелания к монтажу: $ARGUMENTS

Работай от таблицы в `notes/script.md`. Логи ffmpeg — в `work/ffmpeg.log`, в ответ выводи
только код возврата и итоговую длительность. Результат — `work/epNNN_cut.mp4`.
