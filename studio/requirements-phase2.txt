# Фаза 2 — вырезка фона / обложки. Ставится с pypi (модели тянутся с github при первом запуске).
rembg>=2.0
# depth (2.5D параллакс) — тяжёлое, ставить отдельно: pip install torch timm  (Depth-Anything-V2)
